# План миграции на pywebview

Миграция Django-приложения на десктопное приложение на базе `pywebview` с Vue 3 (CDN, без сборки), `sqlite3` и `opencv-python-headless`. Целевая платформа — Windows. Raspberry Pi не требуется.

Статус: `[ ]` — не выполнено, `[x]` — выполнено.

---

## Шаг 1. Подготовка структуры проекта

- [x] Создать папки: `backend/`, `gui/`, `gui/views/`, `gui/css/`, `gui/js/`, `data/`, `tests/`
- [x] Создать пустые файлы: `backend/__init__.py`, `backend/models.py`, `backend/database.py`, `backend/storage.py`, `backend/analyzer.py`, `backend/api.py`, `backend/logger.py`, `main.py`
- [x] Скопировать статические файлы из `analyzer/static/` в `gui/`: `vue.global.prod.js`, `chart.umd.js`, `bootstrap.bundle.min.js`, `bootstrap.min.css`, `main.css`
- [x] Обновить `requirements.txt`: убрать Django и зависимости, добавить `pywebview`, `opencv-python-headless`, `numpy`
- [x] Добавить `data/` и `venv/` в `.gitignore`
- [x] Подготовить тестовые фикстуры: `tests/fixtures/calibration/sources.jpg`, `tests/fixtures/research/1.jpg`, `tests/fixtures/research/2.jpg`

---

## Шаг 2. Backend: логирование (`backend/logger.py`)

- [x] Настроить `logging` из стандартной библиотеки: ротирующий файл `data/app.log` (максимум 5 МБ, 3 резервные копии) + вывод в консоль
- [x] Установить формат записи: `[дата время] [УРОВЕНЬ] [модуль] сообщение`
- [x] Экспортировать один объект `logger` для импорта во все модули
- [x] В `main.py` логировать: старт приложения, версию Python, путь к `data/`
- [x] В `backend/api.py` логировать каждый вызов метода API (имя метода + аргументы без бинарных данных)
- [x] В `backend/analyzer.py` логировать: начало/конец обработки каждого файла, количество найденных контуров, коэффициент калибровки
- [x] В `backend/database.py` логировать ошибки SQL на уровне ERROR
- [x] В `backend/storage.py` логировать файловые операции (копирование, удаление) на уровне DEBUG
- [x] Все необработанные исключения в `api.py` логировать с полным трейсбеком (`logger.exception`)

---

## Шаг 3. Backend: модели данных (`backend/models.py`)

- [x] Реализовать `@dataclass` для `Calibration` (id, name, microscope, coefficient, division_price, date)
- [x] Реализовать `@dataclass` для `Research` (id, name, employee, microscope, calibration_id, average_perimeter, average_area, average_width, average_length, average_dek, date)
- [x] Реализовать `@dataclass` для `ContourData` (research_id, contour_number, perimeter, area, width, length, dek)
- [x] Перенести константы `MICROSCOPES` и `DIVISION_PRICES` из `analyzer/models.py`

---

## Шаг 4. Backend: база данных (`backend/database.py`)

- [x] Реализовать функцию получения соединения с `data/app.db` (путь вычисляется относительно `main.py`)
- [x] Реализовать `init_db()`: создание таблиц `calibration`, `research`, `contour_data` если не существуют
- [x] Реализовать CRUD для калибровок: `get_all_calibrations()`, `get_calibration(id)`, `save_calibration(data)`, `delete_calibration(id)`
- [x] Реализовать CRUD для исследований: `get_all_researches()`, `get_research(id)`, `save_research(data)`, `delete_research(id)`
- [x] Реализовать сохранение и удаление контуров исследования: `save_contours(research_id, contours)`, `delete_contours(research_id)`

---

## Шаг 5. Backend: файловые операции (`backend/storage.py`)

- [x] Реализовать константы путей: `DATA_DIR`, `SESSION_DIR`, `RESEARCH_DIR`, `CALIBRATION_DIR`
- [x] Реализовать `clear_session()`: очистка `data/session/` с пересозданием подпапок
- [x] Реализовать `clear_session_research()` и `clear_session_calibration()` для раздельной очистки
- [x] Реализовать `save_research_files(research_id)`: копирование из `session/research/` в `data/research/<id>/`
- [x] Реализовать `load_research_files(research_id)`: копирование из `data/research/<id>/` в `session/research/`
- [x] Реализовать `save_calibration_files(calibration_id)`: аналогично для калибровки
- [x] Реализовать `load_calibration_files(calibration_id)`: аналогично для калибровки
- [x] Реализовать `delete_research_files(research_id)` и `delete_calibration_files(calibration_id)`
- [x] Реализовать `image_to_base64(path)`: чтение файла и возврат base64-строки для передачи в JS

---

## Шаг 6. Backend: обработка изображений (`backend/analyzer.py`)

- [x] Перенести `increase_contrast()` из `analyzer/views.py` (версия с GaussianBlur)
- [x] Перенести `find_and_draw_contours()` из `analyzer/views.py`
- [x] Перенести `analyze_contours()` из `analyzer/views.py`
- [x] Перенести `pixels_to_real_units()` из `analyzer/views.py`
- [x] Перенести функции калибровки: `find_vertical_lines()`, `detect_black_strips_left_edges()` из `analyzer/views.py`
- [x] Реализовать `run_research_analysis(calibration_coefficient, division_price_value)`: полный пайплайн для всех файлов в `session/research/sources/`, результаты пишет в `session/research/contrasted|contours|analyzed/`
- [x] Реализовать `run_calibration_analysis()`: пайплайн для `session/calibration/sources.jpg`, результаты пишет в `session/calibration/`

---

## Шаг 7. Backend: API-мост (`backend/api.py`)

- [x] Создать класс `Api` с `self.session` для хранения текущего состояния в памяти
- [x] Реализовать методы управления сессией: `new_research()`, `new_calibration()` (очищают сессию и сбрасывают `self.session`)
- [x] Реализовать `upload_image(filename, base64_data, context)`: сохранение изображения из base64 в `session/`
- [x] Реализовать `delete_image(filename, context)`: удаление из `session/`
- [x] Реализовать `list_images(context)`: список файлов в `session/.../sources/`
- [x] Реализовать `get_image(filename, folder, context)`: возврат base64 изображения из `session/`
- [x] Реализовать `execute_research(calibration_id)`: вызов `analyzer.run_research_analysis()`, возврат результатов
- [x] Реализовать `execute_calibration()`: вызов `analyzer.run_calibration_analysis()`, возврат коэффициента
- [x] Реализовать `get_calibrations()`, `get_calibration(id)`, `save_calibration(data)`, `load_calibration(id)`, `delete_calibration(id)`
- [x] Реализовать `get_researches()`, `get_research(id)`, `save_research(data)`, `load_research(id)`, `delete_research(id)`
- [x] Реализовать `get_microscopes()` и `get_division_prices()`: возврат статических списков

---

## Шаг 8. Точка входа (`main.py`)

- [x] Реализовать импорт и инициализацию: `logger`, `database.init_db()`, `storage.clear_session()`
- [x] Создать экземпляр `Api`
- [x] Запустить `webview.create_window()` с путём к `gui/index.html` и объектом `api`
- [x] Запустить `webview.start()` с `debug=True` для разработки

---

## Шаг 9. Frontend: оболочка SPA (`gui/index.html`)

- [x] Создать единственный HTML-файл с подключением Bootstrap, Vue, Chart.js, api.js и всех View-файлов
- [x] Реализовать маршрутизацию между экранами через Vue (без Vue Router — простое переключение `currentView`)
- [x] Убрать все Django-теги, вернуть стандартные `{{ }}` разделители Vue

---

## Шаг 10. Frontend: JS API-обёртка (`gui/api.js`)

- [x] Реализовать функции-обёртки для всех методов `window.pywebview.api.*`
- [x] Добавить единую обработку ошибок

---

## Шаг 10.5. Frontend: переход на компонентную архитектуру

- [ ] Переработать `gui/index.html`: убрать миксины, зарегистрировать `HomeView`, `AnalyzeView`, `ResultsView`, `CalibrationView` как глобальные Vue-компоненты через `app.component(...)`
- [ ] Каждый `gui/views/*.js` теперь экспортирует объект-компонент (`window.HomeView = { template: \`...\`, data() {...}, methods: {...} }`) вместо миксина
- [ ] `index.html` остаётся минимальным: только скелет навигации и теги `<home-view>`, `<analyze-view>`, `<results-view>`, `<calibration-view>`

---

## Шаг 11. Frontend: экран главной (`gui/views/HomeView.js`)

- [ ] Реализовать список исследований с кнопками загрузки и удаления
- [ ] Реализовать переход к экрану анализа при создании нового или загрузке существующего исследования

---

## Шаг 12. Frontend: экран калибровки (`gui/views/CalibrationView.js`)

- [ ] Перенести логику из `analyzer/static/vue/calibration.vue.js`
- [ ] Заменить HTTP-запросы на вызовы `window.pywebview.api.*`
- [ ] Отображение изображений через base64 вместо URL

---

## Шаг 13. Frontend: экран анализа (`gui/views/AnalyzeView.js`)

- [ ] Перенести логику из `analyzer/static/vue/analyze.vue.js`
- [ ] Заменить HTTP-запросы на вызовы `window.pywebview.api.*`
- [ ] Загрузка изображений через base64

---

## Шаг 14. Frontend: экран результатов (`gui/views/ResultsView.js`)

- [ ] Перенести логику из `analyzer/static/vue/results.vue.js`
- [ ] Заменить HTTP-запросы на вызовы `window.pywebview.api.*`

---

## Шаг 14.5. Frontend: экран настроек (`gui/views/SettingsView.js`)

- [ ] Добавить вкладку «Настройки» в навигацию `index.html` (рядом с Калибровкой)
- [ ] Зарегистрировать компонент `SettingsView` в `index.html`
- [ ] Реализовать `gui/views/SettingsView.js`: отображение текущих списков микроскопов и цен делений из `api.getMicroscopes()` / `api.getDivisionPrices()`
- [ ] Предусмотреть место для будущего подключения физических микроскопов (заглушка с сообщением «В разработке»)

---

## Шаг 15. Тесты (`tests/`)

Тестовые фикстуры: `tests/fixtures/calibration/sources.jpg`, `tests/fixtures/research/1.jpg`, `tests/fixtures/research/2.jpg`.

- [ ] `tests/test_analyzer.py` — тесты обработки изображений:
  - `test_increase_contrast`: применить к `research/1.jpg`, проверить что результат — изображение тех же размеров
  - `test_find_and_draw_contours`: проверить что функция возвращает изображение и не падает
  - `test_analyze_contours_returns_results`: проверить что для `research/1.jpg` возвращается непустой список контуров с ключами `perimeter`, `area`, `width`, `length`, `dek`
  - `test_analyze_contours_with_calibration`: проверить что при `calibration_coefficient=10` размеры меняются пропорционально
  - `test_run_calibration_analysis`: скопировать `calibration/sources.jpg` во временную папку сессии, запустить `run_calibration_analysis()`, проверить что коэффициент > 0 и созданы файлы `contrasted.jpg`, `contours.jpg`, `calibrated.jpg`
  - `test_run_research_analysis`: скопировать `research/1.jpg` и `research/2.jpg` во временную папку сессии, запустить `run_research_analysis()`, проверить что возвращены контуры и созданы файлы в `contrasted/`, `contours/`, `analyzed/`
- [ ] `tests/test_database.py` — тесты БД (используют временный `app.db` в `tmp`):
  - `test_init_db`: создаёт схему без ошибок
  - `test_save_and_get_calibration`: сохранить калибровку, получить по id, проверить поля
  - `test_delete_calibration`: сохранить и удалить, убедиться что больше не существует
  - `test_save_and_get_research`: сохранить исследование с контурами, получить, проверить поля и список контуров
  - `test_delete_research_cascades_contours`: удалить исследование, убедиться что контуры тоже удалены
- [ ] `tests/test_storage.py` — тесты файловых операций (используют `tmp_path`):
  - `test_clear_session`: после очистки все подпапки существуют и пусты
  - `test_save_and_load_research_files`: сохранить файлы из сессии, загрузить обратно, проверить совпадение
  - `test_image_to_base64`: передать `calibration/sources.jpg`, проверить что результат — валидная base64-строка
- [ ] Убедиться что все тесты запускаются командой `python -m pytest tests/` без запущенного приложения

---

## Шаг 16. Сборка для Windows (`app.spec`)

- [ ] Создать `app.spec` для PyInstaller с явным указанием `collect_all=['cv2']`
- [ ] Включить папки `gui/` и `tests/fixtures/` в `datas`
- [ ] Проверить сборку командой `pyinstaller app.spec`
- [ ] Проверить запуск собранного `.exe` на чистой машине (или без установленного Python)
